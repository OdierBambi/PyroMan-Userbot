version: "1.0"
services:
  worker:
    build: .
    environment:
      API_ID: $API_ID
      API_HASH: $API_HASH
      ALIVE_LOGO: $ALIVE_LOGO
      DB_URL: $DATABASE_URL
      STRING_SESSION1: $STRING_SESSION1
